// The bitfield base comes from `@discordjs/structures`: `bigint`-backed, with `bitField`, `has`, `any`, `missing`,
// `add`, `remove`, `freeze`, `toArray`, `serialize`, and static `resolve`. Flag bitfields that package does not
// ship yet are derived from it in `./flags.ts`.
export { BitField, type BitFieldResolvable } from "@discordjs/structures";
